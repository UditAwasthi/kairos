# Server
PORT=3000

# Clerk (server-side only — never commit real values)
CLERK_SECRET_KEY=sk_test_your-secret-key-here

# Neon PostgreSQL
# DATABASE_URL  = pooled connection (app runtime) — ends with -pooler
# DIRECT_URL    = direct connection (prisma migrate) — no -pooler
# Both require sslmode=require
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
DIRECT_URL=postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# Object storage root (ephemeral on most PaaS hosts — replace with S3/R2 later)
STORAGE_ROOT=./storage
